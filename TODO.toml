# TODO — active task list for this project.
#
# Single source of truth for active work (see CLAUDE.md -> documentation map).
# Edit through the todo skill so ids, ordering, and metadata stay consistent:
#   scripts/todo.sh <command>      (see .claude/skills/todo/SKILL.md)
# Hand-editing works too — it is plain TOML — but the skill keeps it tidy.

schema_version = 1

[meta]
title = "TODO"
kind = "todo"
purpose = "The live task list for claude_spawner — open work only."
documentation_map = "CLAUDE.md"
archive = "FINISHED.toml"

[meta.rules]
add_proposed_work = "Add proposed features and tests as new tasks rather than losing them."
remove_dropped_items = "Remove dropped items with a one-line why (`scripts/todo.sh remove <id> --reason`)."
finish_work = "When a task is fully finished (built, tested, documented), run `scripts/todo.sh done <id>` to move it to FINISHED.toml — in the same commit that completes the work."
scope = "Both halves of claude_spawner live here: the Go server and the Kotlin/KMP app + web client. The historical phase roadmap stays in README.md."

[[task]]
id = "re-login-to-claude-from-the-app-login-flow"
title = "Re-login to Claude from the app (/login flow)"
description = "Let the user re-authenticate Claude Code without touching the host shell. First-pass scoping found a non-TUI path: the CLI has 'claude auth login [--claudeai|--console|--sso|--email]', 'claude auth logout', and 'claude auth status --json' (returns loggedIn, authMethod, apiProvider, email, orgName, subscriptionType). So we drive 'claude auth', NOT the interactive /login TUI — that keeps the no-TUI-scraping rule intact. Flow: report status, let the user pick subscription vs console, start login, capture the emitted OAuth URL, open it in a browser locally, feed back a pasted code if required, then confirm with 'auth status --json'. Children: probe-claude-auth-login-non-interactive-behavior-in-the-sandbox, design-the-auth-login-driver-seam-in-internal-session, add-auth-status-login-websocket-messages-to-the-gateway, open-the-claude-login-url-in-a-local-browser, client-ui-for-the-re-login-flow, add-a-hey-buddy-log-in-command."
status = "in-progress"
level = "epic"
category = "feature"
urgency = "normal"
order = 10
created = 2026-08-10
tags = ["auth", "session"]

[[task]]
id = "add-a-hey-buddy-log-in-command"
title = "Add a 'hey buddy' log-in command"
description = "Add a voice command that starts the re-login flow (e.g. 'hey buddy, log in to claude' / 'hey buddy, auth status'), following the Registry→docs/commands.json→APK pipeline in docs/commands.md — add it to command.Registry, regenerate with 'go run ./cmd/gencommands', document in docs/commands.md. Spoken reply reads back auth status; starting a login speaks the instruction to check the phone/browser for the link, since a URL cannot be usefully spoken."
status = "active"
level = "task"
category = "feature"
urgency = "low"
order = 70
created = 2026-08-10
tags = ["auth", "voice"]

[[task]]
id = "single-flight-guard-for-in-progress-logins"
title = "Single-flight guard for in-progress logins"
description = "Each 'claude auth login' invocation mints a fresh code_challenge/state, so its URL is bound to that process and starting a second login silently invalidates the first. Guard one live login per host+identity: a second start either returns the existing pending login (with its already-issued URL) or explicitly cancels and replaces it, never leaving two racing. Clean up the process on client disconnect so a killed app does not strand a PTY."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 100
created = 2026-08-10
tags = ["auth", "session"]

[[task]]
id = "document-the-re-login-flow-in-readme-md"
title = "Document the re-login flow in README.md"
description = "User-facing documentation for re-logging into Claude: what the flow does, subscription vs API console, that the OAuth callback is hosted by Anthropic (no local port, so the link can be completed on the phone), and that the pasted code goes back through the app. Link docs/scopes/claude-auth-login.md for the mechanics rather than restating them."
status = "active"
level = "task"
category = "docs"
urgency = "low"
order = 110
created = 2026-08-10
tags = ["auth"]

[[task]]
id = "auth-state-holder-and-inbound-routing-for-the-re-login-flow"
title = "Auth state holder and inbound routing for the re-login flow"
description = "Client plumbing under the UI: route the three inbound auth frames (AuthStatus, AuthLoginUrl, AuthLoginResult — already parsed in net/Protocol.kt) through net/InboundRouter.kt into a per-host auth state holder (status, in-flight login, url, awaiting-code, last error). Add a small AuthController interface next to HostsIdentitiesController (commonMain) exposing requestAuthStatus/startLogin(method)/submitCode/cancelLogin/logout over Outbound.authStatus/authLogin/authLoginCode/authLoginCancel, and implement it on VoiceController so both the app and the web client share it."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 120
created = 2026-08-10
tags = ["android", "auth"]

[[task]]
id = "shared-compose-login-screen-for-a-host"
title = "Shared Compose login screen for a host"
description = "The UI itself, in commonMain so app and browser share it: current status line (logged in as <email>, plan / logged out), a 'log in again' action, subscription-vs-API-console chooser, then the URL step using ui/LinkOpener.kt (open + copy fallback), a paste-code field, progress, and a clear success/failure result that refreshes auth status. Cancel on navigate-away sends authLoginCancel."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 130
created = 2026-08-10
tags = ["android", "auth"]

[[task]]
id = "surface-claude-auth-status-in-hosts-settings"
title = "Surface Claude auth status in Hosts settings"
description = "Give the login screen an entry point: show each host's Claude login status in SettingsHostsIdentities.kt's HostsSettings row (fetched via requestAuthStatus on open) with a tap-through to the login screen, plus a log-out action."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 140
created = 2026-08-10
tags = ["android", "auth"]

[[task]]
id = "offer-re-login-inline-when-a-turn-fails-on-expired-credentials"
title = "Offer re-login inline when a turn fails on expired credentials"
description = "When a turn fails because the host's Claude credentials expired, the error shown in the chat log should offer the re-login flow inline (a tap that opens the login screen for that session's host) rather than leaving the user to find it in settings. Needs the server-side turn_failed reason to be distinguishable as an auth failure — check whether it already is, and make it so if not."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 150
created = 2026-08-10
tags = ["android", "auth"]

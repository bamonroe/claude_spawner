# Go
/server/spawner
/server/*.test
*.out

# Android
/android/.gradle/
/android/build/
/android/app/build/
/android/local.properties
/android/.idea/
*.apk
*.keystore

# Secrets / config
.env
*.ppn
# Containerized server: real env (holds the token) + its state volume (never commit).
/deploy/spawner-container.env
/deploy/state/

# Editor / OS
.DS_Store
*.swp
server/spawner

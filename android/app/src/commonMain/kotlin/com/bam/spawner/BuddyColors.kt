package com.bam.spawner

import androidx.compose.ui.graphics.Color

/**
 * The "buddy orange" accent — the warm attention cue used across the UI (the warm-cache
 * indicator, and in the sidebar for sessions that are thinking or have unread output). Kept
 * in one place so every "needs your attention" signal reads with the same colour.
 */
val BuddyOrange = Color(0xFFF57C00)

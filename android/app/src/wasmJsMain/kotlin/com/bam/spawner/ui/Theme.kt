package com.bam.spawner.ui

import androidx.compose.runtime.Composable

/** Web: browsers own their chrome, so there's no system bar to tint. */
@Composable
actual fun ApplySystemBarAppearance(dark: Boolean) {
}
